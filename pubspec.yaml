name: phone_bridge
description: Phone Bridge
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  shared_preferences: ^2.3.2
  flutter_background_service: ^5.1.0
  flutter_background_service_android: ^6.1.0
  flutter_local_notifications: ^17.0.0
  permission_handler: ^11.3.0
  pointycastle: ^3.9.1
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true